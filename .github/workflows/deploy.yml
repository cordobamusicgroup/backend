name: Deploy to Server

on:
  push:
    branches:
      - main
      - staging

jobs:
  deploy:
    runs-on: kvm4
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }}

    concurrency:
      group: deploy-${{ github.ref_name }} # Agrupar por rama (main o staging)

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up SSH keys and known hosts
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p ${{ secrets.SSH_PORT }} -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      - name: Validate deployment environment variables
        run: |
          if [ -z "${{ vars.DEPLOY_PATH }}" ] || [ -z "${{ vars.ENV }}" ]; then
            echo "❌ Error: DEPLOY_PATH o ENV no están configuradas en GitHub Actions."
            exit 1
          fi
        env:
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH }}
          ENV: ${{ vars.ENV }}

      - name: Debug Environment Variables
        run: |
          echo "📌 DEPLOY_PATH: $DEPLOY_PATH"
          echo "📌 ENV: $ENV"
        env:
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH }}
          ENV: ${{ vars.ENV }}

      - name: Deploy and restart containers on server
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH }}
          ENV: ${{ vars.ENV }}
        run: |
          echo "🚀 Iniciando despliegue en $DEPLOY_PATH..."
          ssh -i ~/.ssh/id_rsa -p $SSH_PORT -o StrictHostKeyChecking=no $SSH_USERNAME@$SSH_HOST << 'EOF'
            
            set -e  # Detener ejecución si hay errores

            # Pasar variables de entorno manualmente dentro de la sesión SSH
            export DEPLOY_PATH='${{ vars.DEPLOY_PATH }}'
            export ENV='${{ vars.ENV }}'

            echo "🖥️ Conectado a: $(hostname)"
            echo "📂 DEPLOY_PATH: $DEPLOY_PATH"
            echo "📌 ENV: $ENV"

            if [ ! -d "$DEPLOY_PATH" ]; then
              echo "❌ Error: DEPLOY_PATH no existe."
              exit 1
            fi

            cd "$DEPLOY_PATH"
            echo "✅ Ubicación actual: $(pwd)"

            if [ ! -d ".git" ]; then
              echo "❌ Error: No es un repositorio Git."
              exit 1
            fi

            echo "🔄 Restableciendo cambios locales y actualizando código..."
            git reset --hard HEAD
            git clean -fd  # Elimina archivos no rastreados
            
            BRANCH=$(git rev-parse --abbrev-ref HEAD)
            echo "📌 Usando rama: $BRANCH"
            
            git fetch origin $BRANCH
            git pull origin $BRANCH --rebase

            echo "🚀 Reiniciando contenedores..."
            chmod +x start_containers.sh
            ./start_containers.sh "$ENV"

            echo "✅ Despliegue completado."
          EOF
